<!DOCTYPE html>
<html lang="en">
<head>
  <title>Sauron</title>
  <%= stylesheet_link_tag "bootstrap.min", "application", :media => "all" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Sauron", root_url, class: 'brand' %>
      <ul class="nav">
        <li><%= link_to "Conversations", conversations_path %></li>
      </ul>
      <%= form_tag search_messages_url, method: :get, class: "navbar-search pull-right" do %>
        <%= label_tag :q, 'Search' %>
        <%= text_field_tag :q, params[:q], class: 'search-query', placeholder: 'Search messages...' %>
        <%= submit_tag 'Search', class: 'submit-button' %>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
  <p id="session">Logged in as <span class="username"><%= current_username %></span></p>
  <%= yield %>
</div>

</body>
</html>
