%h2.name= @contact.name
%h3.email= @contact.email

=link_to "Edit", edit_contact_path(@contact)

%h2 Messages

%ul
- @contact.messages.each do |message|
  %li
    %span.timestamp= message.date.to_s(:full)
    = link_to message.subject, message.message_thread